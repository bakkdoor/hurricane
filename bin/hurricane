#!/usr/bin/env fancy

require: "../lib/storm"

topology_file = ARGV[1]

if: topology_file then: {
  Storm in_definition: {
    require: topology_file
  }
} else: {
  System abort: "No file given. Aborting."
}